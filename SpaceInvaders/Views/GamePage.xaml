<Page x:Class="SpaceInvaders.Views.GamePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:converters="using:SpaceInvaders.Views.Converters"
      xmlns:models="using:SpaceInvaders.Models.GameObjects"
      xmlns:viewModels="using:SpaceInvaders.ViewModels"
      Background="#000011"
      Loaded="Page_Loaded"
      Unloaded="Page_Unloaded"
      KeyDown="Page_KeyDown"
      KeyUp="Page_KeyUp"
      IsTabStop="True">

    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:EnemyTypeToImageConverter x:Key="EnemyTypeToImageConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </Page.Resources>

    <Grid>
        <!-- Overlay Inicial de START -->
        <Border x:Name="StartOverlay"
                Background="#80000000"
                Visibility="{Binding IsStartOverlayVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
                <TextBlock Text="SPACE INVADERS"
                           Foreground="#FFFFFF"
                           FontSize="48"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"/>
                <Button Content="START GAME"
                        Width="200"
                        Height="50"
                        FontSize="18"
                        Command="{Binding StartGameCommand}"/>
            </StackPanel>
        </Border>

        <!-- CAMADA 1: Game Canvas -->
        <Canvas x:Name="GameCanvas"
                Width="800"
                Height="600"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="#000022">

            <!-- Player -->
            <Image x:Name="PlayerImage"
                   Width="50" Height="30"
                   Source="ms-appx:///Assets/Images/player.png"
                   Canvas.Left="{Binding GameState.Player.Bounds.Left, Mode=OneWay}"
                   Canvas.Top="{Binding GameState.Player.Bounds.Top, Mode=OneWay}"/>

            <!-- Enemies Container -->
            <ItemsControl x:Name="EnemiesCanvas"
                          ItemsSource="{Binding GameState.Enemies, Mode=OneWay}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><Canvas/></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="models:Enemy">
                        <Image Width="{Binding Bounds.Width, Mode=OneWay}"
                               Height="{Binding Bounds.Height, Mode=OneWay}"
                               Source="{Binding Type, Converter={StaticResource EnemyTypeToImageConverter}, Mode=OneWay}"
                               Canvas.Left="{Binding Bounds.Left, Mode=OneWay}"
                               Canvas.Top="{Binding Bounds.Top, Mode=OneWay}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Red Enemy -->
            <Image x:Name="RedEnemyImage"
                   Width="40" Height="30"
                   Source="ms-appx:///Assets/Images/invaderRED.gif"
                   Visibility="{Binding GameState.RedEnemy, Converter={StaticResource NullToVisibilityConverter}, Mode=OneWay}"
                   Canvas.Left="{Binding GameState.RedEnemy.Bounds.Left, Mode=OneWay}"
                   Canvas.Top="{Binding GameState.RedEnemy.Bounds.Top, Mode=OneWay}"/>

            <!-- Bullets Container -->
            <ItemsControl x:Name="BulletsCanvas"
                          ItemsSource="{Binding GameState.Bullets, Mode=OneWay}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><Canvas/></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="models:Bullet">
                        <Rectangle Width="3" Height="10"
                                   Fill="{Binding Type, Converter={StaticResource BulletTypeToBrushConverter}, Mode=OneWay}"
                                   Canvas.Left="{Binding Bounds.Left, Mode=OneWay}"
                                   Canvas.Top="{Binding Bounds.Top, Mode=OneWay}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Shields Container -->
            <ItemsControl x:Name="ShieldsCanvas"
                          ItemsSource="{Binding GameState.Shields, Mode=OneWay}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><Canvas/></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="models:Shield">
                        <Rectangle Width="{Binding Bounds.Width, Mode=OneWay}"
                                   Height="{Binding Bounds.Height, Mode=OneWay}"
                                   Fill="{Binding Health, Converter={StaticResource ShieldHealthToBrushConverter}, Mode=OneWay}"
                                   Canvas.Left="{Binding Bounds.Left, Mode=OneWay}"
                                   Canvas.Top="{Binding Bounds.Top, Mode=OneWay}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </Canvas>

        <!-- CAMADA 2: HUD Superior -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Margin="20,10,0,0"
                    Spacing="30">
            <TextBlock Text="{Binding FormattedScore}" Foreground="#FFFFFF" FontSize="16" FontFamily="Consolas"/>
            <TextBlock Text="{Binding FormattedLives}" Foreground="#FFFFFF" FontSize="16" FontFamily="Consolas"/>
            <TextBlock Text="{Binding FormattedLevel}" Foreground="#FFFFFF" FontSize="16" FontFamily="Consolas"/>
        </StackPanel>

        <!-- CAMADA 3: Overlay de Pause -->
        <Border x:Name="PauseOverlay"
                Background="#80000000"
                Visibility="{Binding GameState.IsPaused, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="GAME PAUSED" Foreground="#FFFFFF" FontSize="48" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Text="Press P to continue" Foreground="#AAAAAA" FontSize="18" HorizontalAlignment="Center" Margin="0,10"/>
            </StackPanel>
        </Border>

        <!-- CAMADA 4: Overlay de Game Over -->
        <Border x:Name="GameOverOverlay"
                Background="#80000000"
                Visibility="{Binding GameState.IsGameOver, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
                <TextBlock Text="GAME OVER" Foreground="#FF4444" FontSize="48" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding FormattedFinalScore}" Foreground="#FFFFFF" FontSize="24" HorizontalAlignment="Center"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="10">
                    <TextBlock Text="Your Name:" Foreground="#FFFFFF" VerticalAlignment="Center"/>
                    <TextBox x:Name="PlayerNameTextBox" Width="200" PlaceholderText="Enter your name" MaxLength="15"/>
                </StackPanel>

                <Button x:Name="SaveScoreButton"
                        Content="SAVE SCORE"
                        Command="{Binding SaveScoreCommand}"
                        CommandParameter="{Binding ElementName=PlayerNameTextBox, Path=Text}"
                        Width="200" Height="50" FontSize="16"/>
                <Button x:Name="RestartButton"
                        Content="PLAY AGAIN"
                        Command="{Binding RestartCommand}"
                        Width="200" Height="50" FontSize="16"/>
                <Button x:Name="MenuButton"
                        Content="MAIN MENU"
                        Command="{Binding MainMenuCommand}"
                        Width="200" Height="50" FontSize="16"/>
            </StackPanel>
        </Border>

        <!-- CAMADA 5: Controles -->
        <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="20" Spacing="10">
            <TextBlock Text="Controls:" Foreground="#AAAAAA" FontSize="12"/>
            <TextBlock Text="← → Move" Foreground="#AAAAAA" FontSize="10"/>
            <TextBlock Text="SPACE Shoot" Foreground="#AAAAAA" FontSize="10"/>
            <TextBlock Text="P Pause" Foreground="#AAAAAA" FontSize="10"/>
            <TextBlock Text="ESC Menu" Foreground="#AAAAAA" FontSize="10"/>
        </StackPanel>
    </Grid>
</Page>
